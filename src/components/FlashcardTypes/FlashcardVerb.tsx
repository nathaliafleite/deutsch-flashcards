import { Verb } from '../../app/helpers/types';

type Props = {
    data: Verb;
    flip: boolean;
    handleFlip: () => void;
};

const FlashcardVerb: React.FC<Props> = () => {
    return <></>;
};

export default FlashcardVerb;
